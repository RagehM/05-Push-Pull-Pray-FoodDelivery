info:
  name: GetMostUsedOffers
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8085/api/payments/offers/top-used?limit=10
  params:
    - name: limit
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

