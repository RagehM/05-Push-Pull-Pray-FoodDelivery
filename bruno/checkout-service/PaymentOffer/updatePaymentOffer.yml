info:
  name: UpdatePaymentOffer
  type: http
  seq: 5

http:
  method: PUT
  url: http://localhost:8085/api/payments/offers/1
  body:
    type: json
    data: |-
      {
        "paymentId": 1,
        "offerId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

