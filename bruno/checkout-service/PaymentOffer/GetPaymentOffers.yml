info:
  name: GetPaymentOffers
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8085/api/payments/offers
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
