info:
  name: GetOfferById
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8085/api/offers/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
