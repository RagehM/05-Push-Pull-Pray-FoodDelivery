info:
  name: GetPayments
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8085/api/payments
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
