info:
  name: RetryFailedPayment
  type: http
  seq: 11

http:
  method: PUT
  url: http://localhost:8085/api/payments/1/retry
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

