info:
  name: Search role ADMIN
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8081/api/users/search?role=ADMIN
  params:
    - name: role
      value: ADMIN
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
