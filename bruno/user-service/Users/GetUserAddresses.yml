info:
  name: GetUserAddresses
  type: http
  seq: 15

http:
  method: GET
  url: http://localhost:8081/api/users/1/addresses
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

