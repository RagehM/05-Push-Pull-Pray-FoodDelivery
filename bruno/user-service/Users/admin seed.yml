info:
  name: admin seed
  type: http
  seq: 19

http:
  method: GET
  url: http://localhost:8081/api/seed
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
