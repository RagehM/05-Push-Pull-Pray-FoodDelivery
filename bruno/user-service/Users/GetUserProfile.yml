info:
  name: GetUserProfile
  type: http
  seq: 13

http:
  method: GET
  url: http://localhost:8081/api/users/1/profile
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

