info:
  name: UpdateUserPreferences
  type: http
  seq: 7

http:
  method: PUT
  url: http://localhost:8081/api/users/1/preferences
  body:
    type: json
    data: |-
      {
        "language": "ar",
        "dietaryRestrictions": "vegan",
        "notifications": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

