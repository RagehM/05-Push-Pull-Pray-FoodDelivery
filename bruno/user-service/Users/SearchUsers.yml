info:
  name: SearchUsers
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8081/api/users/search?name=Ali
  params:
    - name: name
      value: Ali
      type: query
      enabled: false
    - name: email
      value: ali@example.com
      type: query
      enabled: false
    - name: role
      value: CUSTOMER
      type: query
      enabled: false
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

