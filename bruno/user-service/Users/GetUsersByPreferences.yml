info:
  name: GetUsersByPreferences
  type: http
  seq: 9

http:
  method: GET
  url: http://localhost:8081/api/users/preferences/search?key=language&value=en
  params:
    - name: key
      value: language
      type: query
    - name: value
      value: en
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

