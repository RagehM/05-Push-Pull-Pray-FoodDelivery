info:
  name: update user prefrences
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8081/api/users/:id/preferences
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: '{"language":"en","dietaryRestrictions":"none"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
