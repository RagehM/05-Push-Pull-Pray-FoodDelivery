info:
  name: Login
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8081/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "rageh2@msail.com",
        "password": 1234656
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
