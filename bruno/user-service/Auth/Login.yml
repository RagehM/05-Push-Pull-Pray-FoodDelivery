info:
  name: Login
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8081/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "rageh112@mail.com",
        "password": 123
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
