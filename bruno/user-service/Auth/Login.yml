info:
  name: Login
  type: http
  seq: 4

http:
  method: POST
  url: "{{kube_base_url_api_gateway}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "ragdaasdseh@mail.com",
        "password": 123,
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
