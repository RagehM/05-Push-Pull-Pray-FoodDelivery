info:
  name: fetch restaurants
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8082/api/restaurants
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
