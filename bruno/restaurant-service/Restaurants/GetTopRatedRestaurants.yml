info:
  name: GetTopRatedRestaurants
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:8082/api/restaurants/reports/top-rated?limit=5
  params:
    - name: limit
      value: "5"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

