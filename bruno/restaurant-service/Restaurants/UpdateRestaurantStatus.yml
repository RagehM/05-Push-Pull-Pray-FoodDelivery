info:
  name: UpdateRestaurantStatus
  type: http
  seq: 9

http:
  method: PUT
  url: http://localhost:8082/api/restaurants/1/status
  body:
    type: json
    data: |-
      {
        "status": "SUSPENDED"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

