info:
  name: EstimateOrder
  type: http
  seq: 13

http:
  method: POST
  url: http://localhost:8083/api/orders/estimate
  body:
    type: json
    data: |-
      {
        "restaurantId": 1,
        "items": [
          { "menuItemId": 1, "quantity": 2 },
          { "menuItemId": 2, "quantity": 1 }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

