info:
  name: SearchOrdersByMetadata
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8083/api/orders/metadata/search?key=source&value=mobile
  params:
    - name: key
      value: source
      type: query
    - name: value
      value: mobile
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

