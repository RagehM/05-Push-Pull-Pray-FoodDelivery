info:
  name: UpdateOrderItem
  type: http
  seq: 5

http:
  method: PUT
  url: http://localhost:8083/api/orderItems/update/1
  body:
    type: json
    data: |-
      {
        "menuItemId": 1,
        "quantity": 3,
        "unitPrice": 12.5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

