info:
  name: CreateOrderItem
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8083/api/orderItems/1
  body:
    type: json
    data: |-
      {
        "menuItemId": 1,
        "quantity": 2,
        "unitPrice": 12.5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

