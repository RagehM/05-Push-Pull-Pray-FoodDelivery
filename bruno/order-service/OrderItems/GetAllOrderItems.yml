info:
  name: GetAllOrderItems
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8083/api/orderItems
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

