info:
  name: CreateDelivery
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8084/api/deliveries
  body:
    type: json
    data: |-
      {
        "orderId": 1,
        "driverName": "John Doe",
        "status": "PENDING",
        "estimatedArrival": 30.0,
        "metadata": {}
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

