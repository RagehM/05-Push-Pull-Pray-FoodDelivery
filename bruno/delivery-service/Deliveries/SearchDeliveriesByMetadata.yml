info:
  name: SearchDeliveriesByMetadata
  type: http
  seq: 9

http:
  method: GET
  url: http://localhost:8084/api/deliveries/metadata/search?key=zone&operator=eq&value=north
  params:
    - name: key
      value: zone
      type: query
    - name: operator
      value: eq
      type: query
    - name: value
      value: north
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

