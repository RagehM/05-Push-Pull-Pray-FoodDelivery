info:
  name: GetLatestDeliveryByOrderId
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:8084/api/deliveries/order/1/latest
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

