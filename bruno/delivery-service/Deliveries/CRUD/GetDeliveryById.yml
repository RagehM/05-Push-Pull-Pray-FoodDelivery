info:
  name: GetDeliveryById
  type: http
  seq: 1

http:
  method: GET
  url: "{{DELIVERY_SERVICE_URL}}/api/deliveries/1"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("get delivery by id returns 200 and has id", function () {
          expect(res.getStatus()).to.equal(200);
          const body = res.getBody();
          expect(body).to.have.property('id');
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
