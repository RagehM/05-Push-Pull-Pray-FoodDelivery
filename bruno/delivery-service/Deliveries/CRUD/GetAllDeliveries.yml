info:
  name: GetAllDeliveries
  type: http
  seq: 2

http:
  method: GET
  url: "{{kube_base_url_api_gateway}}/api/deliveries"
  auth:
    type: bearer
    token: eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiJyYWdkYWFzZHNlaEBtYWlsLmNvbSIsInVpZCI6MSwicm9sZSI6IkNVU1RPTUVSIiwiaWF0IjoxNzc5MTE0MTUyLCJleHAiOjE3NzkyMDA1NTJ9.a6MHpcjUKwreJspnCuo5lwk7q09MI-tbZUxmW1OF5ac

runtime:
  variables:
    - name: DELIVERY_SERVICE_URL
      value: http://localhost:8084
  scripts:
    - type: tests
      code: |-
        test("get all deliveries returns array", function () {
          expect(res.getStatus()).to.equal(200);
          const body = res.getBody();
          expect(body).to.be.an('array');
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
