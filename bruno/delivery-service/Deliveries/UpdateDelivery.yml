info:
  name: UpdateDelivery
  type: http
  seq: 5

http:
  method: PUT
  url: http://localhost:8084/api/deliveries/1
  body:
    type: json
    data: |-
      {
        "driverName": "Jane Smith",
        "status": "IN_PROGRESS",
        "estimatedArrival": 20.0,
        "metadata": {}
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

