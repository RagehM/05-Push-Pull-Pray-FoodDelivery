info:
  name: GetDelayedDeliveries
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:8084/api/deliveries/delayed?maxEstimatedArrival=45.0&sinceMinutes=30
  params:
    - name: maxEstimatedArrival
      value: "45.0"
      type: query
    - name: sinceMinutes
      value: "30"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

