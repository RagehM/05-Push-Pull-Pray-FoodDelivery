services:
  postgres:
    build:
      context: .
      dockerfile: postgres.Dockerfile
    container_name: fooddelivery-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: fooddeliverydb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - pgdata:/var/lib/postgresql
    # healthcheck:
    #   test: ["CMD-SHELL", "test -f /tmp/healthy"]
    #   interval: 10s
    #   timeout: 5s
    #   retries: 10
    #   start_period: 15s
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d fooddeliverydb"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  pgadmin:
    build:
      context: .
      dockerfile: pgadmin.Dockerfile
    container_name: fooddelivery-pgadmin
    ports:
      - "9090:80"
    depends_on:
      - postgres
    restart: on-failure
    volumes:
      - pgadmin-data:/var/lib/pgadmin

  mongo:
    image: mongo:latest
    container_name: fooddelivery-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: rootpass
      MONGO_INITDB_DATABASE: fooddeliverymongo
    volumes:
      - mongo-data:/data/db
    healthcheck:
      test: [ "CMD", "mongosh", "--eval", "db.adminCommand('ping')" ]
      interval: 10s
      timeout: 5s
      retries: 5

  mongo-express:
    image: mongo-express:latest
    container_name: fooddelivery-mongo-ui
    ports:
      - "27018:8081"
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: root
      ME_CONFIG_MONGODB_ADMINPASSWORD: rootpass
      ME_CONFIG_MONGODB_URL: mongodb://root:rootpass@mongo:27017/
      ME_CONFIG_BASICAUTH: "false"
    depends_on:
      mongo:
        condition: service_healthy

  redis:
    image: redis:latest
    container_name: fooddelivery-redis
    ports:
      - "6379:6379"
    command: >
      redis-server
      --requirepass redispass
      --maxmemory 256mb
      --maxmemory-policy allkeys-lru
    healthcheck:
      test: [ "CMD", "redis-cli", "-a", "redispass", "ping" ]
      interval: 5s
      timeout: 5s
      retries: 5

  redisinsight:
    image: redis/redisinsight:latest
    container_name: fooddelivery-redis-ui
    ports:
      - "5540:5540"
    depends_on:
      - redis

  elasticsearch:
    image: elasticsearch:9.0.3
    container_name: fooddelivery-elasticsearch
    ports:
      - "9200:9200"
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - ES_JAVA_OPTS=-Xms512m -Xmx512m
    volumes:
      - es-data:/usr/share/elasticsearch/data
    healthcheck:
      test: [ "CMD-SHELL", "curl -f http://localhost:9200/_cluster/health || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  kibana:
    image: kibana:9.0.3
    container_name: fooddelivery-kibana
    ports:
      - "5601:5601"
    environment:
      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200
    depends_on:
      elasticsearch:
        condition: service_healthy

  neo4j:
    image: neo4j:latest
    container_name: fooddelivery-neo4j
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      NEO4J_AUTH: neo4j/neo4jpass
      NEO4J_server_memory_heap_max__size: 512m
      NEO4J_server_memory_heap_initial__size: 256m
    volumes:
      - neo4j-data:/data
    healthcheck:
      test: [ "CMD", "neo4j", "status" ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  cassandra:
    image: cassandra:latest
    container_name: fooddelivery-cassandra
    ports:
      - "9042:9042"
    environment:
      CASSANDRA_CLUSTER_NAME: fooddeliverycluster
      CASSANDRA_DC: datacenter1
      CASSANDRA_KEYSPACE: fooddeliveryks
      JVM_OPTS: "-Xms256M -Xmx512M"
      MAX_HEAP_SIZE: 512M
      HEAP_NEWSIZE: 128M
    volumes:
      - cassandra-data:/var/lib/cassandra
    command: >
      bash -c "
        docker-entrypoint.sh cassandra -f &
        until cqlsh -e 'DESCRIBE KEYSPACES' 2>/dev/null; do sleep 5; done;
        cqlsh -e \"CREATE KEYSPACE IF NOT EXISTS $${CASSANDRA_KEYSPACE} WITH replication = {'class':'SimpleStrategy','replication_factor':1};\";
        wait
      "
    healthcheck:
      test: [ "CMD-SHELL", "cqlsh -e 'DESCRIBE KEYSPACES'" ]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 60s

  order-service:
    build:
      context: ./order-service
      dockerfile: Dockerfile
    container_name: fooddelivery-order-service
    ports:
      - "8083:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/fooddeliverydb
      SPRING_MONGODB_URI: mongodb://root:rootpass@mongo:27017/fooddeliverymongo?authSource=admin
      SPRING_NEO4J_URI: bolt://neo4j:7687
      JWT_SECRET: ${JWT_SECRET}
    depends_on:
      postgres:
        condition: service_healthy

  checkout-service:
    build:
      context: ./checkout-service
      dockerfile: Dockerfile
    container_name: fooddelivery-checkout-service
    ports:
      - "8085:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/fooddeliverydb
      JWT_SECRET: ${JWT_SECRET}
    depends_on:
      postgres:
        condition: service_healthy

  delivery-service:
    build:
      context: ./delivery-service
      dockerfile: Dockerfile
    container_name: fooddelivery-delivery-service
    ports:
      - "8084:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/fooddeliverydb
      JWT_SECRET: ${JWT_SECRET}
    depends_on:
      postgres:
        condition: service_healthy
    entrypoint: [ "sh", "-c", "sleep 90 && exec java -jar app.jar" ]

  user-service:
    build:
      context: ./user-service
      dockerfile: Dockerfile
    container_name: fooddelivery-user-service
    ports:
      - "8081:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/fooddeliverydb
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRATION: ${JWT_EXPIRATION}
    depends_on:
      postgres:
        condition: service_healthy

  restaurant-service:
    build:
      context: ./restaurant-service
      dockerfile: Dockerfile
    container_name: fooddelivery-restaurant-service
    ports:
      - "8082:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/fooddeliverydb
      JWT_SECRET: ${JWT_SECRET}
    depends_on:
      postgres:
        condition: service_healthy

volumes:
    pgdata:
    pgadmin-data:
    mongo-data:
    es-data:
    neo4j-data:
    cassandra-data:


