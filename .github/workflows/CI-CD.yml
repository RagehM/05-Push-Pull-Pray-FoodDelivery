name: CI/CD Pipeline

on:
  push:
    branches:
      - "feat/**"
      - main

jobs:
  build-and-test:
    name: Maven Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 25 (Temurin)
        uses: actions/setup-java@v4
        with:
          java-version: "25"
          distribution: temurin
          cache: maven

      - name: Build & run tests
        run: mvn -B clean install -DskipTests

  docker-build:
    name: Docker Build (feat only)
    runs-on: ubuntu-latest
    needs: build-and-test
    if: startsWith(github.ref, 'refs/heads/feat/')

    strategy:
      fail-fast: false
      matrix:
        include:
          - service: api-gateway
            context: .
            dockerfile: api-gateway/Dockerfile
          - service: user-service
            context: .
            dockerfile: user-service/Dockerfile
          - service: restaurant-service
            context: .
            dockerfile: restaurant-service/Dockerfile
          - service: order-service
            context: .
            dockerfile: order-service/Dockerfile
          - service: delivery-service
            context: .
            dockerfile: delivery-service/Dockerfile
          - service: checkout-service
            context: .
            dockerfile: checkout-service/Dockerfile

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Docker image (no push)
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: false
          tags: |
            local/${{ matrix.service }}:latest

  docker-push:
    name: Docker Push (main only)
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.ref == 'refs/heads/main'

    permissions:
      contents: read
      packages: write

    strategy:
      fail-fast: false
      matrix:
        include:
          - service: api-gateway
            context: .
            dockerfile: api-gateway/Dockerfile
          - service: user-service
            context: .
            dockerfile: user-service/Dockerfile
          - service: restaurant-service
            context: .
            dockerfile: restaurant-service/Dockerfile
          - service: order-service
            context: .
            dockerfile: order-service/Dockerfile
          - service: delivery-service
            context: .
            dockerfile: delivery-service/Dockerfile
          - service: checkout-service
            context: .
            dockerfile: checkout-service/Dockerfile

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Lowercase owner
        id: vars
        run: echo "owner=$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: true
          tags: |
            ghcr.io/${{ steps.vars.outputs.owner }}/fooddelivery-${{ matrix.service }}:latest
            ghcr.io/${{ steps.vars.outputs.owner }}/fooddelivery-${{ matrix.service }}:${{ github.sha }}