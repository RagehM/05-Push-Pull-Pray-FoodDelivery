apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: checkout-postgres-data
  namespace: talabat
  labels:
    app: checkout-postgres
    component: database
    feature: S5-READ-DB
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 2Gi
