apiVersion: v1
kind: Service
metadata:
  name: loki
  namespace: monitoring
  labels:
    app: loki
    component: monitoring
spec:
  clusterIP: None
  selector:
    app: loki
  ports:
    - port: 3100
      targetPort: 3100
      name: http
