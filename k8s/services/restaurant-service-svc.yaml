apiVersion: v1
kind: Service
metadata:
  name: restaurant-service
  namespace: talabat
spec:
  type: ClusterIP
  selector:
    app: restaurant-service
  ports:
    - port: 8080
      targetPort: 8080
