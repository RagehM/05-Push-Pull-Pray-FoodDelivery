apiVersion: v1
kind: Service
metadata:
  name: restaurant-postgres
  namespace: talabat
spec:
  clusterIP: None
  selector:
    app: restaurant-postgres
  ports:
    - port: 5432
      targetPort: 5432
