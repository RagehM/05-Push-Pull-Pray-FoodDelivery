apiVersion: v1
kind: Service
metadata:
  name: checkout-service
  namespace: talabat
spec:
  type: ClusterIP
  selector:
    app: checkout-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080

